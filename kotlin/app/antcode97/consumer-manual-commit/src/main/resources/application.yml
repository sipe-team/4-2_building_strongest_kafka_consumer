server:
  port: 8082

spring:
  application:
    name: kafka-consumer-manual-commit
  
  kafka:
    bootstrap-servers: localhost:9092
    
    # 수동 커밋 컨슈머 설정 - 카프카 핵심 가이드 p.150-170 참조
    consumer:
      # 기본 설정
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      
      # 그룹 ID 설정
      group-id: manual-commit-consumer-group
      
      # 오프셋 리셋 정책 - 카프카 핵심 가이드 p.127
      auto-offset-reset: earliest
      
      # 수동 커밋 설정 - 카프카 핵심 가이드 p.155
      enable-auto-commit: false  # 자동 커밋 비활성화
      auto-commit-interval: 0ms  # 자동 커밋 간격 0
      
      # 세션 타임아웃 설정
      session-timeout: 30000ms
      heartbeat-interval: 3000ms
      
      # 메시지 가져오기 설정
      fetch-min-size: 1
      fetch-max-wait: 500ms
      max-poll-records: 500
      
      # 연결 설정
      reconnect-backoff: 50ms
      retry-backoff: 100ms
      
      # 고급 설정
      max-poll-interval: 300000ms  # 5분
      request-timeout: 30000ms
      
      # 파티션 할당 전략 - 카프카 핵심 가이드 p.135
      partition-assignment-strategy: org.apache.kafka.clients.consumer.RangeAssignor

logging:
  level:
    org.apache.kafka: INFO
    team.sipe.antcode97.consumermanualcommit: DEBUG
    org.springframework.kafka: DEBUG 